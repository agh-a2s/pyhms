import unittest
from unittest.mock import MagicMock

#TODO: this set of tests is more complicated to implement. Add mocks for the tree and deme classes
class UnitTestSprout(unittest.TestCase):

    def test_if_sprout_is_working(self):
        self.assertTrue(True, "Sprout is not working")